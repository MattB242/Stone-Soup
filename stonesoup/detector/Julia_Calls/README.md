This folder contains the project and manifest files that should be in your Julia environment 
if you wish to use the Bayesian_DoA detector object in Bayesian_DoA.py, along with a small module of helper files

In Bayesian_DoA.py, this should be set as env path
